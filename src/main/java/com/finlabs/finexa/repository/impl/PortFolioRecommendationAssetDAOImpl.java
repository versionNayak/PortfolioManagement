package com.finlabs.finexa.repository.impl;

import com.finlabs.finexa.repository.PortFolioRecommendationAssetDAO;

//@Repository
public class PortFolioRecommendationAssetDAOImpl implements PortFolioRecommendationAssetDAO {
	// @Autowired
	// GenericDao genericDao;

	/*
	 * @SuppressWarnings("unchecked")
	 * 
	 * @Override public List<MasterProductRecommendation>
	 * geMasterProductRecommendationList(int subAssetClassId) throws
	 * FinexaDaoException { List<MasterProductRecommendation>
	 * masterProductRecommendationList = null; try { masterProductRecommendationList
	 * = genericDao.getSession() .createQuery(
	 * "SELECT mpr FROM MasterProductRecommendation mpr WHERE mpr.lookupAssetSubClass.id=:subAssetClassId"
	 * ) .setByte("subAssetClassId", (byte) subAssetClassId).list();
	 * 
	 * return masterProductRecommendationList; } catch (Exception e) { throw new
	 * FinexaDaoException(e.getMessage());
	 * 
	 * } }
	 */

}
